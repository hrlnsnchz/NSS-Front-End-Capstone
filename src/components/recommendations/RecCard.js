import {Link} from "react-router-dom"

export const RecCard = ({media}) => {
    return (
        <section className="mediaCard">
            <h3>
          {/* { media.name } */}
        </h3>
        </section>
    )
}